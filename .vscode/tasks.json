{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build MNIST Project",
            "type": "shell",
            "command": "clang++ -std=c++17 -O3 -mcpu=native -Wall src/*.cpp -Iinclude $(pkg-config --cflags --libs opencv4) -o main.out",
            "group": {
                "kind": "build"
            },
            "problemMatcher": ["$gcc"]
        },

        {
            "label": "Build Tests",
            "type": "shell",
            "command": "clang++ -std=c++17 -O3 -mcpu=native -Wall tests/*.cpp $(find src -name '*.cpp' ! -name 'main.cpp') -Iinclude $(pkg-config --cflags --libs opencv4) -o tests.out",
            "group": {
                "kind": "build"
            },
            "problemMatcher": ["$gcc"]
        },

        {
            "label": "Run Benchmark Tests",
            "type": "shell",
            "command": "clang++ -std=c++17 -O3 -mcpu=native benchmarks/*.cpp $(find src -name '*.cpp' ! -name 'main.cpp') -Iinclude -o benchmark.out",
            "group": {
                "kind": "build"
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
